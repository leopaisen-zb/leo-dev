# Pre-change entry retrieval

Independent `gpt-5.6-terra` / `medium` actor, read-only. Source: the preserved pre-P3 snapshot, not the new contract. It read `skills/develop/SKILL.md` and `references/{lifecycle,upstream-methods,delivery}.md` under `/private/tmp/leo-dev-spec-revision-before.Zjd5S0`.

The actor correctly reported that this version does not support adopting a revised Spec/constitution into an already routed dependent plan, replacing routed tasks, or selectively retaining evidence. It recommended preserving old code/history/approvals, doing impact analysis, and using a separately approved independent change if needed. It distinguished expired-submission review recovery from Spec revision and did not invent a command, refresh a hash or reuse stale evidence.

No controller command or test was run by the actor. This is a demonstrated reference/capability gap, not an agent discipline failure and not evidence of a reasoning improvement. The new instruction work is a conditional reference to an actually implemented interface; no blanket prohibition table or large persuasion test is warranted.
