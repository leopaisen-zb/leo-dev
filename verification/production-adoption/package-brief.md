# P1 — packaged upstream resource closure

Task contract: existing sole plan, section “C3.4 production adoption execution contract — 2026-09-11”, P1. Read that paragraph and its shared constraints; do not read the whole historical plan.

Own only `scripts/build-adapters.mjs`, new `scripts/upstream-resources.mjs`, `tests/adapters/` changes, mechanical resource copies under `skills/develop/references/upstream/{cc-sdd,spec-kit}` and `provenance.json` there. Main owns all workflow instructions, components/NOTICE and new `references/upstream-methods.md`; include that file in package inventory but do not author it. Controller worker owns all CLI production/tests. You are not alone; preserve all other edits. No children, Git writes, installation, network, cache/global edits or historical experiment changes. Use apply_patch for edits and mechanical copies; templates remain originals, not additional public skills.

Copy the 17 files listed in `experiments/reuse-first/provenance.json`, from its trialPath, preserving bytes. New paths are `references/upstream/<repository>/<sourcePath>` relative to skills/develop. Production provenance contains `schemaVersion: 1`, a truthful production-resource-only scope, and `sources` with repository/revision/sourcePath/sourceSha256/transformation/packagedPath/packagedSha256. Retain original license files. Do not depend on experiments at runtime/build time after copying. Source-resource inventory remains fixed and strict; new helper should export that explicit inventory and validate manifest and byte hashes. Permit only none and the known append-final-newline-only transformation, checking original bytes accordingly; reject malformed/duplicate/escaping paths, changed bytes/hash mismatch/missing resources and symlinks using existing path defenses. Existing verify-packages uses portableFiles, so avoid a new verification engine.

TDD first: add a build/relocation test that fails because built packages lack resources/provenance; save actual RED. Then implement and verify all four portable packages contain the pinned exact resources/licenses and no experimental material/extra public skill. Test source tamper/manifest path invalidity and packaged tamper refusal on temporary copies, never source corruption. Existing adapter suite must still pass. Main will supply upstream-methods.md; coordinate rather than fabricate a placeholder. Test output and report `verification/production-adoption/package-report.md`; return status/test summary/concerns only. Review package will use a before filesystem snapshot because repository has no HEAD. No commit requirement applies.
