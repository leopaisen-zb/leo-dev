# P2 — assessed dependent plan admission

Task contract: existing sole plan, section “C3.4 production adoption execution contract — 2026-09-11”, P2. Read that paragraph and shared constraints, not the whole historical plan.

You own controller/governance route code, narrow command/type changes needed for this public seam, and focused CLI tests. Do not edit state/revision machinery, lifecycle docs, package scripts or resources. Main owns workflow docs; separate worker owns packaging. You are not alone: preserve all edits, no children, Git mutation, installs, external writes, changed acceptance/budgets, caches or old trial/fixture changes. Use apply_patch, TDD and actual compiled CLI tests.

Known gap: controller.ts rejects plan+assessment in dry-run and real route. Reuse governanceAdmission/assess/governanceRefusal and existing atomic batches/history. Exact identity: `plan:` plus canonical fingerprint of the validated normalized tasks array, preserving order and all task properties. Return a public `state.planAssessmentContext` from route --plan --dry-run (including scope in `taskId` and current change/spec/tree hashes; keep existing state.assessmentContext). Callers need not reproduce hashing. Dry-run with no assessment must expose context even if a prior assessment means admission is refused, while retaining refusal status/code; never write. Scope change must not bypass existing history/material barriers; single-task assessment cannot pass for a whole plan. Validate plan before scope/admission, same real/dry normalization. Record ready plan-scope assessment atomically with routes; refusals record assessment only. Legacy unassessed plans remain labelled not-assessed. Expose state/diagnostics without new lifecycle states or schema expansion unless necessary; report questions.

REDs: complete ready assessment admits two dependent tasks; partial/unknown/local-high/material refusals with no routes; omitted assessment cannot bypass recorded refusal; linked supersession; stale different-plan assessment; dry-run exact barriers/context and zero writes; legacy compatibility; recovery atomicity with existing tests/helpers. Preserve existing warnings/status semantics and auth. No spec revision in this task.

Run focused tests after build, typecheck; main runs broad regressions later. Preserve RED/GREEN and test commands in `verification/production-adoption/governance-report.md`. No HEAD exists, so no Git diff claim. Main supplies before snapshot for independent review; report changed paths/test totals/concerns and actual public JSON example.
