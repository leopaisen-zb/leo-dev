# Consumer evidence addendum

The initial response remains unedited in consumer-initial.md. The same actor was asked to check its claim that the Codex package lacks a runnable controller, using the built runtime and codex-team reference without executing the fixture. It retracted that claim: runtime/runtime-manifest.json declares Node >=20 and packages/cli/dist/index.js; runtime/packages/cli/package.json declares the binary; the compiled controller/commands are present; codex-team.md provides the local entry binding. This is a same-session factual correction, not another independent successful sample. Runtime operation was not tested by that actor. Main subsequently ran the packaged `route --help` and got a valid HELP envelope with --plan/--assessment/--dry-run.

Main's initial assessment: original resource retrieval, no duplicate specification/approval, contract-overconstraint detection, required-vs-optional tests and unavailable producer evidence were demonstrated on this one fixture. Main initially treated the consumer's F2 as a legitimate unavailable-executable-context observation, not proof of a production defect. Independent review instead classified its readiness demand as unsupported stricter acceptance because the frozen fixture required coverage categories only. Ruling after comparing the frozen protocol: retain the literal missing-context observation, but F2 cannot become an additional mandatory readiness criterion for this fixture; preserve it as a sample defect, not a reason to rewrite the fixture. Its initial runtime-absence statement was also an error. No instruction tuning/fixture changes or repeated sampling were done to manufacture a clean pass. Overall consumer smoke remains partial; neither prompt reliability nor improvement over native reasoning is proven (the baseline already caught the contract issue).

The consumer read an intermediate package before the packaging-only SKILL.md → RESOURCE.md rename and latest assessed-plan lifecycle documentation. The delivery directory was rebuilt for those changes. Resource instruction bytes remained pinned; final filename/link checks belong to package tests and independent package review, not a retroactive claim that the first consumer saw the final package. Fixture spec/constitution/design/tasks are unchanged.

The actor's generic rule/template list also lacks complete exact per-resource paths/revisions in its response. The production manifest and independent reviewer establish the 17-file source/packaged byte chain, but do not retroactively prove which bytes the actor consumed. This is an additional sample traceability limitation; the earlier actual 3-arm consumption trial remains separate evidence, not a substitute.

Final public-entry sentinel decision used official OpenAI documentation (https://learn.chatgpt.com/docs/build-skills), which identifies SKILL.md as a skill entry but does not specify nested-reference exclusion. A recursive artifact test now requires exactly one sentinel; this is preventive packaging, not an observed client-loader failure. Public-document lookup did not start a browser, proxy or daemon. No client was installed or reconfigured.
