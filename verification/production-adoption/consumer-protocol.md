# Production-entry consumer check (frozen before dispatch)

Baseline: `/root/entry_baseline`, requested Terra/medium, fresh context, read-only pre-change snapshot. It could not discover original cc-sdd/Spec Kit resources. It correctly rejected unsupported successful-JSON byte/order constraints and kept unavailable producer evidence unverified. Thus retrieval absence is observed RED; review judgment is a pre-existing successful control, not demonstrated improvement. The earlier S-trial false positives/real overconstraint remain under verification/reuse-first without modification.

Forward input: `/private/tmp/leo-dev-production-consumer.VHd3G5/{spec,constitution,design,tasks}.md`, frozen by creation before actor dispatch. Built entry will be under `/private/tmp/leo-dev-production-delivery.IAbsP6/codex/leo-dev/skills/develop/SKILL.md`. Actor sees only fixture + built entry/reachable resources, not historical trial/current-source reports, evaluator outcomes or this protocol. No implementation, children, installs, approvals, hooks, or writes outside a designated response artifact.

Task: review the prepared draft package for readiness using whichever bundled original methods are useful, preserving approved requirements. Report actual resources read and mappings, findings by class, and next action. Explain whether full/parallel cc-sdd methods are available if this grew complex; do not claim they ran.

Acceptance: original cc-sdd task-generation and Spec Kit analyze resources and necessary references can actually be read from the built entry without checkout/install; preserve R IDs/canonical files/approval; detect invented byte/order success restriction; distinguish missing producer report from absent implementation; retain mandatory subprocess validation and optional duplicate tests; report read-only/draft and unsupported full/parallel resource limits. A single sample is a retrieval/application smoke, not model reliability, full upstream invocation, or installed-client acceptance. Independent final reviewer/evaluator assesses raw output, not just actor claims. No test threshold is changed after dispatch.

Initial local validation: `npx vitest run tests/skill-contract` yielded 5 passes/1 inventory failure at the new upstream-methods link; narrow expected-inventory update retained exact inventory validation, then 6/6 passed. Python quick_validate initially could not import yaml in either default/system Python; retry with already cached PyYAML via command-local PYTHONPATH passed, with no dependency installation or global change.
