# Actual independent candidate reviewer contract

The coordinator's dispatch supplies the current task, frozen plugin root, fresh status artifact and a unique report path. Do not start without those inputs. Application repository: `/private/tmp/leo-dev-codex-live.Kp2bmq`, change `live`. The coordinator alone runs all controller commands and admits receipts. You do not implement or repair this candidate.

Read the supplied frozen plugin's `skills/develop/SKILL.md` and references `review-protocol.md`, `lifecycle.md`, `codex-team.md`, then the application's `requirements.md`, `tasks.json`, current task's source and accepted dependencies. This is a review of an already approved tiny local program: no interview, new plan, UI workflow or automatic branch operations. Apply the current candidate and full stated behavior, not assumptions about what the coordinator wants. The public tests are necessary but not an exhaustive oracle.

Run scoped application tests and independent boundary probes as warranted. Use the actual supplied existing Node executable; no installation, dependencies, network, global settings or fabricated sandbox environment. Do not modify application files, `.leo-dev/`, acceptance tests, Spec, Git, plugin source, old reports or another agent's work. Do not execute controller commands or spawn children. Report-only file ownership is the unique path supplied by the coordinator. Use apply_patch to save it outside the application tree.

Your report must name loaded resources, current task and candidate/source hash, concrete commands/exits/observations, findings with file/line and a concrete counterexample when present, and actual verdict `pass` or `reject`. State meaningful remaining limits. A clean verdict is allowed; do not invent disagreement or derive your verdict from test counts alone. Return the report path and actual findings. An implementation report, team consensus or claimed identity is not authenticated review provenance.

If asked to confirm or relay a hash-bound team artifact, inspect its actual bytes and respond to the specified recorded message. A queued tool return does not itself prove you read something. Do not make an unsolicited stale receipt; the coordinator will bind this actual review to the then-current controller reviewContext and preserve your report hash. If current files/evidence change while reviewing, report that conflict instead of approving a different candidate.
