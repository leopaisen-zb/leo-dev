# Original-fixture continuation protocol

Frozen before continuation mutations, 2026-09-10. Extends the approved C3.3 plan and the original `verification/codex-execution/live-20260910/protocol.md`; does not change controlled application requirements, task plan, fixed tests or oracle.

## Starting evidence

Original fixture `/private/tmp/leo-dev-codex-live.Kp2bmq`, change `live`; preserved before snapshot `/private/tmp/leo-dev-review-recovery-before.5OZhSJ/live-fixture`. A remains review-required, original Run succeeded, lease generation 1 expired, B/C pending. Original successful A source/candidate/Gate/submission and failed review-admission evidence remain historical facts. Baseline current-source read and 8 existing execution tests are in `baseline.json`.

Use the same explicit controller Node executable `/Users/leo/.nvm/versions/node/v22.22.2/bin/node`; preserve the existing Gate registry with its original Node25 test argv. Do not weaken launcher fingerprint validation. After source review, main freshly compiles and uses that settled source/runtime; record the path and hashes. No install or default configuration change.

## Acceptance sequence

1. Main compares current immutable input/source/Git/journal hashes with the before snapshot. Recover **original A** using the new public command. Assert same Run/lease/candidate/Gate hashes, no additional Gate execution, no budget consumption, B/C still pending, recovery epoch published. Do not reset the fixture or reimplement A.
2. Start a genuinely independent new reviewer with the source Skill, original requirements, A candidate and a freshly captured coordinator-provided CLI status artifact. Members do not execute controller commands; they may inspect the persisted read-only evidence and run scoped application tests/probes. Do not provide the expected verdict or old review conclusion. Main binds the real findings to the fresh reviewContext, uses agent-asserted provenance, and records the actual review result. A must become done before B is claimable.
3. Main uses the accepted Codex team interface for real actor/thread bindings and hash-bound review/implementation correspondence. Coordinator owns all controller mutations; workers edit only assigned source files. Actual host forwarding/body responses are distinguished from pending/delivered records. Team opinions do not bypass the review command.
4. Main claims B with actual implementer identity and the existing 600000 ms test lease. A real worker implements B within `src/cart.mjs` and reports its changes/commands. Main then injects the previously specified quantity-coercion defect explicitly, retaining before/after bytes. This deliberate mutation is not attributed to the implementer. The frozen public Gate may pass; an independent reviewer receives actual candidate/full requirements but not the mutation recipe or expected outcome.
5. Record actual B rejection through current reviewContext. Assert C remains pending/unclaimable, one shared failure is counted and old lease released. A real fresh/remediation worker repairs within the fixed acceptance and budget; run Gate, submit and independent review again. Only accepted B unlocks C.
6. C uses a distinct implementation context as required by the original protocol. After B is accepted and its writer has stopped, main saves a hash-bound handoff and explicitly replaces the logical writer binding with C's actual fresh thread. This is a planned responsibility handoff, not a claim that the still-reachable B thread crashed or disappeared; do not fabricate host-loss evidence. Main claims C with that actual implementer identity. Worker writes only `src/cli.mjs`, then stops before candidate registration. A different fresh continuation member reads the persisted task/Run and source and assesses the existing candidate; do not regenerate code blindly. Main performs candidate verification using the original Run after that handoff, then independent C review. This is controlled context interruption, not an OS crash claim.
7. Run the unchanged six public tests and held-out oracle. Verify all tasks done, no active/unknown Run or active lease, and change integration-review (not release-ready). Verify original immutable requirements/tests/task plan/Gate registry/note and Git HEAD/index preserved. Preserve all actual failures and unrun steps if a prerequisite fails.

Receipts and review reports remain under this verification directory, outside the application tree; team correspondence copies/requests are under its `.leo-dev/runtime/live/` (excluded from candidate hashing). That additive team storage is the C3 adaptation, not an edit to immutable application inputs. Do not forge platform-attested/human-confirmed identity, edit journal/lease timestamps, increase a timeout to conceal a failure, change acceptance, or introduce network/dependencies/Git writes.

Record actual tool-accepted agent model/effort/identity, loaded resources, command exits/output, timestamps and source hashes. No cost/token metric is invented when unavailable. One successful sequence demonstrates this case only, not unattended reliability, installed-client discovery, actual client crash recovery or full-stack/training/Agent readiness.

## Reference validation

The old source guidance plus real A review previously reached the expiry refusal and correctly reported it; missing recovery capability/reference is the baseline, not a discipline failure. The narrow new API-reference instructions will be validated by actual fresh reviewer/continuation resource consumption and public command behavior in this sequence. No generic prohibition expansion or multi-repetition wording experiment is needed for this reference addition.
