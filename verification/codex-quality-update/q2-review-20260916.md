# Q2 independent review — 2026-09-16

## Verdict

**PASS.** I found no remaining Q2 blocker. The candidate is ready for the root Q2 decision and the next phase. This review was read-only with respect to product source, configuration, tests, thresholds and recorded results; I did not author the Q2 implementation.

The conclusion is limited to the approved Q2 slice: deterministic test discovery, scoped correctness lint, a reporting-only design/recovery coverage run, CI wiring and an explicit fault-model disposition. It is not a claim of repository-wide branch completeness or cross-platform reliability.

## Dependency and runtime contract

- `package.json` adds only the two quality scripts and four exact dev dependencies: `@eslint/js@10.0.1`, `eslint@10.10.0`, `typescript-eslint@8.70.0` and `@vitest/coverage-v8@3.2.7`.
- The lockfile resolves Vitest to `3.2.7`, and the coverage provider declares peer `vitest: 3.2.7`; the provider and runner therefore match exactly under `npm ci`.
- The lockfile's ESLint 10 family requires Node `^20.19.0 || ^22.13.0 || >=24`. The recorded quality host is Node `v22.22.2`, so the development toolchain is compatible with the actual host.
- Comparison with `verification/codex-quality-update/before/package.json.txt` and `before/package-lock.json.txt` shows the package runtime contract is retained: root `engines.node` remains `>=20`, root runtime dependency `ajv-formats` remains `3.0.1`, and the `packages/cli` dependency map is unchanged. The documentation consistently distinguishes Node 22 development checks from the Node 20-or-newer packaged runtime.

## Configuration and failure behavior

- `eslint.config.mjs` applies the recommended JavaScript and TypeScript correctness rules to exactly four design-boundary files: `design-admission.ts`, `design-events.ts`, `design-policy.ts` and `schema/validate.ts`. This is the approved focused correctness scope rather than a repository-wide style conversion.
- `vitest.config.ts` discovers only `tests/**/*.test.ts`. The recorded discovery contains all 26 current TypeScript test files under `tests/` and no extra path. Historical `.txt` copies and private experiment test trees outside `tests/` cannot enter that set.
- The two current Node test files, `tests/adapters/codex-runtime.test.mjs` and `tests/adapters/walking-skeleton.test.mjs`, remain outside Vitest discovery and are still executed by the unchanged `node --test tests/**/*.test.mjs` stage in `test:controller`.
- `vitest.quality.config.ts` names eight required Q1/fault files explicitly and sets `fileParallelism: false`. It covers only the four approved design/schema modules, uses V8, and defines no threshold.
- The transcript-derived initial run records exit status 1 when one required test failed. That is direct evidence that a required-file failure makes `test:quality-coverage` fail. The correction serialized the mixed CLI suite; it did not change receipt lifetimes, assertions, product code or the required set.
- GitHub Actions runs on macOS with Node 22, uses ordinary failing shell steps for `lint:quality` and `test:quality-coverage`, and retains typecheck, the full `npm test`, Python tests, example Node tests, adapter build, package verification and marketplace build. There is no `continue-on-error` or other suppression around the quality steps. No separate injected ESLint-error run was recorded; standard ESLint nonzero exit behavior plus the unsuppressed CI step is sufficient for this configuration check.

## Executable evidence

The final raw coverage log reached a normal Vitest terminal summary:

- 8 of 8 required test files passed;
- 123 of 123 tests passed;
- duration 555.23 seconds;
- V8 report for the four configured files: 87.11% statements/lines, 82.75% branches and 94.73% functions.

The focused lint and typecheck logs contain their invoked commands and no diagnostics. The source and quality discovery logs list the expected 26-file and 8-file sets. The raw coverage log does not append a separate shell exit-code marker, but its completed pass summary, coverage table and generated `coverage/quality/coverage-final.json` establish that Vitest reached its successful reporting phase. I did not repeat the nine-minute suite.

## Critical-branch disposition

The coverage JSON reports zero worker-process hits on branches in `design-admission.ts`, `design-policy.ts`, `design-events.ts` and `validate.ts`. Those zeros are not a reliable inventory of missing behavior because the principal public regressions spawn compiled child Node processes, whose V8 data is not attributed to the Vitest worker's TypeScript coverage. Representative apparently uncovered source decisions are nevertheless exercised by required public tests: stale or drifting design evidence, independent and unconsumed receipts, caller-CWD ordinary receipt resolution, repository-contained fresh paths, expiry before preparation, accepted historical preparation, exact receipt-byte capture, malformed latest events, and third-value projection refusal.

The fault matrix maps the high-consequence durable boundaries to named executable cases: prepared-batch invisibility and recovery, exact journal-tail commit/refusal, incomplete-tail handling, stale-generation fencing, repeated recovery without duplicate effects, third-value projection refusal, and initialization staging cleanup. Those cases are in the explicit eight-file set and passed in the final run. I found no uncovered critical branch that requires another Q2 test.

The remaining storage limits are stated accurately: no per-system-call ENOSPC/EIO injection, machine power-loss proof, filesystem-firmware guarantee, or guarantee that every orphan temporary file is reclaimed. Native host interruption belongs to Q3. These limits do not contradict the Q2 contract and should not be converted into implicit pass claims.

## Documentation and evaluation limits

README, CONTRIBUTING, installation and release-evidence text consistently state that coverage is reporting-only and that child-process CLI behavior is underrepresented by worker V8 coverage. They also keep the earlier 472-test release claim attached to its earlier candidate rather than attributing it to this update. The current Q2 evidence does not include Agent traces, retrieval/tool-routing behavior, token usage or model cost; those are outside this software quality configuration and belong to the later actor evaluation. The measured Q2 evidence does not support a recommendation for a stronger model, higher reasoning effort, RAG or multi-agent orchestration.
