# Q1b fix 2 bounded rereview — 2026-09-15

Scope: independent read-only review of the Q1b fix2 report, accepted Q1a order evidence, current controller/service/test delta, and focused execution logs. No source, test, configuration, oracle, threshold, or prior evidence was changed.

## Verdict

**No remaining Q1b blocker found. Q1b is ready for Q2.** Root retains the final phase decision, and Q2/Q4 remain responsible for broader configured verification.

## Compatibility verification

### Fresh-claim order is restored

The service and controller now implement the accepted Q1a sequence:

1. `claimDesignReceiptPlan` rejects Lite routes and missing raw prior-receipt history.
2. Its `prepareReceipt` callback invokes controller-owned `prepareContainedDesignReceipt`, which resolves from the repository root, realpaths the target, and enforces containment without reading receipt bytes.
3. `approveDesignReview` checks authority, the latest request context, and current design-source bytes.
4. Only then does `acquireReceipt` call controller-owned `acquirePreparedDesignReceipt`, which reads, parses, and captures the canonical receipt once.
5. The service checks receipt reuse, binding, and independence; the controller subsequently applies revision-receipt reservation.

This matches the accepted Q1a ordering at the previously ambiguous boundary. Path preparation happens before current context/source validation, while the receipt read remains after it. The path object passed from preparation to acquisition contains the already-canonical absolute path, so the service does not acquire filesystem ownership or reconstruct the path.

The recorded public collision is decisive and consistent with the source:

- The isolated Q1a-accepted controller returned exit 2 / `VALIDATION_ERROR`, with no journal or snapshot writes, for an escaping fresh-receipt path combined with a drifted design source.
- The pre-fix current controller returned exit 5 / `CONFLICT`, proving the regression.
- The unchanged focused test passes after fix2 with exit 2 / `VALIDATION_ERROR` and the same no-write guard.

### Byte identity remains single-capture

`prepareContainedDesignReceipt` records only canonical path metadata. `acquirePreparedDesignReceipt` performs the sole file read through `readReceipt`, receives the exact `Buffer`, and returns it unchanged. `claimDesignReceiptPlan` hashes and Base64-encodes that same buffer. There is no reread between parsing and construction of the embedded batch source.

The existing public read-race test passed after fix2: replacing the file after `readReceipt` did not replace the bytes bound into the planned operation, and the later mismatch was safely blocked without journal/snapshot mutation. This is the relevant regression check; another mirror test is unnecessary.

### Ordinary caller-CWD behavior remains intact

Ordinary design approval still uses the separate `acquireDesignReceipt` path. It computes metadata with `resolve(reference)` and passes the raw reference to the unchanged `readReceipt`, so relative paths resolve from the invoking process cwd and absolute or otherwise arbitrary readable paths remain allowed. It does not call repository containment preparation. The Q1b fix1 shadow-file test remains in the public design suite; fix2 does not alter that branch.

### Decoder scope matches the accepted behavior

Ordinary approval and fresh approval with a newly supplied receipt now request only `latestDesignReviewContext`, matching Q1a and avoiding validation of an unused historical receipt before the applicable path/context checks. Aggregate history decoding remains in consumers that use an existing approved receipt or validate durable recovery/status history. Error translation remains through the existing `DesignAdmissionError` to `ControllerError` boundary.

## Delta and evidence

The fix is limited to the existing Q1b ownership:

- Controller: separate ordinary acquisition from fresh path preparation and prepared acquisition; add the narrow latest-context accessor.
- Design-admission service: accept distinct prepare/acquire callbacks and a raw prior-receipt predicate; retain decision ownership and exact output shape.
- Public continuation test: add the demonstrated path/source collision case.

Recorded checks all pass:

- `npm run typecheck` — passed.
- `npm run build` — passed.
- Focused public collision regression — passed.
- Focused fresh receipt single-capture/embedded-batch regression — passed.

No whole-suite result is inferred from these focused checks, and no duplicate run is counted as additional coverage. I found no path-resolution, byte-binding, ownership, or public error-precedence regression that requires another Q1b repair.
