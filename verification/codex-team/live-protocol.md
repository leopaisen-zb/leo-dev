# Real Codex team protocol — frozen before actor dispatch

Fixture: `/private/tmp/leo-dev-team-live.JYxzv6`. Non-production read-only engineering case, built/source plugin use explicitly identified. Input `spec.md` and `src/cart.mjs` define a simple synchronous integer-cent quote contract with seeded implementation defects. `test/cart.test.mjs` supplies fixed evidence. Participants may find uncovered cases; they may not edit acceptance, implementation or unrelated files. No paid API, external writes or global changes.

The user requested and authorized true Codex team implementation/testing. This exercise opens an initialized change in triage for correspondence only; it does not fabricate a human approval or task completion receipt.

## Frozen assertions

1. Start architect and independent reviewer as distinct actual Codex threads from the team reference, with no prior verdict. Record runtime/source hash, current actual tools/model parameters and returned handles. Read-only shared-tree roles are not claimed physically sandboxed.
2. Persist each original contribution in the fixture runtime, with actual hashes in team journal. No synthetic third-person dialogue.
3. Record challenge pending, actually deliver it with host messaging/followup and retain recipient's response evidence; only then ack. At least one targeted question/reply must connect the independent findings, but a disagreement or manufactured defect is not required.
4. Fresh CLI processes retrieve identical roster/history. Lost-member recovery is a controlled simulation: retain actual old handle, declare it unavailable **to the subsequent coordinator**, create a real fresh thread with only persisted handoff, bind the new generation and refuse a deliberately late old-generation message. Do not claim actual host crash or that an interrupted thread was destroyed. The scenario must be labelled simulated loss + real reconstruction.
5. A new-context consumer locates the packaged/source reference and uses actual commands, without a pasted intended answer. Record missing runtime/commands as failure, not tool-name role-play.
6. Ordinary lifecycle remains triage with no tasks completed and no review receipt produced by the team. Spec/application/tests/user note/Git bytes remain unchanged. This proves correspondence and host collaboration only; task-gate integration and expired lease recovery remain separately required C3.3/C3.4 acceptance.

Baseline before actor dispatch: `node --test test/cart.test.mjs` returned exit 1; 3 tests, 1 pass/2 fail (missing RangeError for fractional quantity and NaN price). The overflow assertion follows the failing NaN assertion and was not reached; a participant may test it separately. Initial source hashes: spec `1d75cd390e255fcd069ff115a1c1c98a3a989ffbe92fe960685a7869424dd1f3`; cart `157995e7739895aa078d68ed7a1a5b36525c3cff1f0b3143304baffcdf06ebc5`; tests `a06aa538a1487dd165063a8891c2afc19aa062ec182d3dce4247701ab3ff4624`; unrelated note `e0ce261edfb023cfd0065c3ab1ebdf3338786580ef4147f8ec94be33c61673c5`.

Reports distinguish deterministic tests, this single behavioral sample, relocatable package execution and installed-client loading. No statistical error-reduction claim follows from this sample.
