# M5 independent assessment brief

Root launches the assigned reviewers only after the retained Mochi Board v2 M5 integration Run has passed its public Gate and is submitted for review. This is verification-only work. Read `/Users/leo/plugins/leo-dev/verification/release-2026-09-14/mochi-config.json` for the actual fixture, fixed Node and verified package CLI; obtain fresh public `status --change mochi-board --repo <root> --json`. Require M5/revision2 and its current reviewContext. Preserve all source, oracle, controller records, task budgets and global settings. No Git, receipt ingestion, controller mutations, new services, subagents or changed thresholds.

Read the current approved `docs/spec-v2.md`, `docs/design.md` and `plan-v2.json`, then the source and evidence relevant to your assigned area. Record actual source hashes and exact candidate/reviewContext before and after. The canonical tree includes exact controller projections; runtime evidence is excluded. Host thread identity comes from the outer trace or the actual collaboration handle, never a guessed ID.

Each reviewer owns only its explicitly assigned evidence directory and must return PASS or NEEDS_FIXES for its area with concrete evidence and limits:

- **Architecture:** backend state ownership, shared validation, serialized atomic persistence, import transaction boundaries, dependency and requirement coverage, compatibility of M4 with M1–M3, verification-only integration. Inspect code and the frozen HTTP result. Run a small targeted independent check only when it resolves a real concern. Do not rewrite architecture for preferences.
- **Security:** fixed loopback/static paths, request Origin and JSON body boundaries, invalid snapshot atomic refusal, prototype/HTML injection boundaries, corrupt disk refusal and absence of unneeded network or credentials. Distinguish a reproducible contract defect from a hypothetical new feature requirement. Use temporary data outside source for any bounded checks and stop exact owned processes.
- **NFR:** actual browser keyboard/focus/error/390px behavior, normal console/network, local-only operation, startup/shutdown/persistence evidence and practical responsiveness. Reuse your own current M4 browser evidence only after exact application-byte and current candidate binding checks. A final screenshot/metric gap must remain visible.

Reports must describe what was inspected versus actually executed. Implementation evidence alone is not an independent pass; reviewers must make their own judgment. No fabricated benchmark, CI or authentication claims. The three actual area sessions must be distinct from each other, M5 producer and primary reviewer. Root records their actual completion timestamps and report hashes, then obtains a separate primary whole-change review and ingests the Full receipt only if the actual evidence passes.
