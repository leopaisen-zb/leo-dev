# Domain docs

Leo Dev is currently a single-context repository.

Before changing domain behavior, read `CONTEXT.md` when it exists and the relevant ADRs under `docs/adr/`. Use their glossary consistently and surface any conflict with an existing ADR instead of silently overriding it.

Domain documentation is created lazily when domain terms or durable architectural decisions need it. Its absence is not an implementation blocker.

