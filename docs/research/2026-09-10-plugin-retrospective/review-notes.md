# 复盘的反证审查

本文件是主线程对实际审查反馈的摘要，不是原始工具 stdout、代码测试结果或独立产品验收。

## 第一轮：源码事实与需求适配，分别审查

`/root/upstream_loop_read`、`/root/upstream_engine_read` 均使用显式 `gpt-5.6-terra` / `high`、独立上下文，无子代理再派发、无实现修改。先核读不同上游，随后对复盘主张分别做证据/语料批评和需求适配/反面论证。主线程同时读当前产品接线及另几个上游，未重复实现其任务。

| 批评 | 主线程处理 |
|---|---|
| 不要因产品未完成就说八仓从没读过；过去报告明示只读关键链路 | 保留“确实下载/读过，但非穷尽理解/完整集成”的区分 |
| Leo Dev 已有 journal、batch、fencing、candidate、串行计划；不能再建议加一个 ledger | 明确只保留一个完成权，反对新增第二账本/引擎 |
| 自研并非整体错误；G1 是批准过的局部准入实现 | 增补公平边界，批评组合与验证顺序，不否认局部交付 |
| 多任务已经支持，但综合 assessment + plan 不支持 | 精确描述 C2 Lite 的能力交集，不再说只有单任务 |
| 修租约可能局部可行，但 TTL 加长或无条件跳转不安全 | 写明代次隔离、候选/Gate 保留、review 新鲜度、晚到结果和预算条件；不预断重写 |
| 源码闭环不等于安装即用；入口/版本发现也应属于验收 | 把 controller 可发现性与源码/安装入口区分前置 |
| “建议整合”不能冒充已批准的实施方案 | 明确本轮只研究，不选替换架构、不安装、不修改生产 |
| 小样例无质量、成本、延迟的统计结论 | 保留 C1 基线已通过、无 A/B 改善证据和实时失败记录 |

## 第二轮：主线程回查与文档一致性

- 对照真实源码核验了 cc-sdd 的 auto-commit、Superpowers commit-range review、BMAD verification-gap 预验证例外、bridge 的 complete 告警而不阻断等边界；不把所有上游吹成硬门禁。
- 把“没有 SDK 自动执行”与“宿主驱动不能自主”区分；后者并不成立，不要求为本产品增设 daemon。
- 保留两个歧义仓库名，不把相关新 bridge 偷换为用户已确认的对象。
- 将历史测试结果与本轮静态阅读分开；本轮没有重跑或宣称再次通过 310 项测试。
- 本地相对链接、来源 JSON 与生产文件哈希的实际机械检查另记录在 `checks.json`。这些检查只验证文档引用/来源/文件保留，不验证产品行为。

未决：具体上游适配合同和恢复语义尚未批准；当前产品仍未通过真实完整任务链。研究审查通过不能关闭这个产品阻塞。
